#include <stdio.h>
#include<math.h>

int main()
{
    int t,n,i,k,l,s;
	scanf("%d",&t);
	for(i=0;i<t;i++)
	{
		scanf("%d",&n);
		/*the survivor is the 'n'th odd number after a corresponding power of 2.*/  	
		k=(log(n))/log(2);
		l=n-pow(2,k);
		s=2*l+1;
		printf("%d\n",s);
		}
    return 0;
}
